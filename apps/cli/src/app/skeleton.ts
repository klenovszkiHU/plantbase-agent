/**
 * Az `apps/cli` a plantbase parancssori belépési pontja (vékony I/O réteg).
 * A2-ben ez még csak egy skeleton-banner; az `ask` parancs és az interaktív
 * mód az A6/B fázisokban épül rá. Tiszta függvény, hogy egységgel tesztelhető.
 */
export function skeletonBanner(): string {
  return 'plantbase cli — skeleton (A2), még nincs parancs implementálva';
}
